# Upper bound is deliberate: mcp 2.x renamed FastMCP to MCPServer, dropped the
# mcp.server.fastmcp module entirely, and moved host/port from .settings onto
# run(). server.py targets the 1.x API. Without the bound a fresh install
# resolves to 2.x and the server fails to import.
mcp>=1.0.0,<2
